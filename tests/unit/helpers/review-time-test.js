import { reviewTime } from 'review-project/helpers/review-time';
import { module, test } from 'qunit';

module('Unit | Helper | review time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reviewTime([42]);
  assert.ok(result);
});
