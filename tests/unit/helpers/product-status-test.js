import { productStatus } from 'review-project/helpers/product-status';
import { module, test } from 'qunit';

module('Unit | Helper | product status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productStatus([42]);
  assert.ok(result);
});
